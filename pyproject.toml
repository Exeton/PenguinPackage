[project]
name = "PenguinPackage"

version = "0.0.1"
description = "Tools for downloading and working with Linux packages"

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

